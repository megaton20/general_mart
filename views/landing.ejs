

  <%- include('./partials/openNav') %>

<!-- and closed if session is found -->
    <main class="main">
      <div class="container">
        <%- include ('./partials/messages') %>
      </div>




    <div class="container mt-4">
      <div class="row">
        <!-- Category Cards (Disappears on mobile) -->
        <div class="col-4 d-none d-md-block">
          <div id="service-details" class="service-details section">
            <div class="card-body">
                <div class="service-box mb-4">
                    <div class="services-list">
                      <% allCategory.forEach(data => { %>
                      <a href="/user/category/<%= data.Category_name %>#product-section" class=" categoryButton" data-category="<%= data.Category_name %>">
                        <i class="bi bi-arrow-right-circle"></i><span> <%= data.Category_name %></span>
                      </a>
                     
                      <% }); %>
                    </div>
                  </div>
            </div>
          </div>
          <!-- Add more category cards as needed -->
        </div>
  
        <!-- Carousel (Turns into col-12 on mobile) -->
        <div class="col-12 col-md-8">
            <div id="carouselExample" class="carousel slide carousel-fade" data-bs-ride="carousel">
              <div class="carousel-inner">
                <div class="carousel-item active">
                  <img src="/landing-imgs/wwwww.JPG" class="d-block w-100" alt="Slide 1">
                </div>
                <div class="carousel-item">
                  <img src="/landing-imgs/zzzzz.JPG" class="d-block w-100" alt="Slide 2">
                </div>
                <div class="carousel-item">
                  <img src="/landing-imgs/banner-2.png" class="d-block w-100" alt="Slide 3">
                </div>
                <div class="carousel-item">
                  <img src="/landing-imgs/banner.png" class="d-block w-100" alt="Slide 4">
                </div>
                <div class="carousel-item">
                  <img src="/landing-imgs/vvvvv.jpg" class="d-block w-100" alt="Slide 5">
                </div>
              </div>
              <button class="carousel-control-prev" type="button" data-bs-target="#carouselExample" data-bs-slide="prev">
                <span class="carousel-control-prev-icon" aria-hidden="true"></span>
                <span class="visually-hidden">Previous</span>
              </button>
              <button class="carousel-control-next" type="button" data-bs-target="#carouselExample" data-bs-slide="next">
                <span class="carousel-control-next-icon" aria-hidden="true"></span>
                <span class="visually-hidden">Next</span>
              </button>
            </div>
          </div>
        </div>
      </div>


      <!-- <div class="container mt-5 mb-4 d-none d-md-block">
        <div class="landing-grid ">

          <a href="/user/category/#" class=" categoryButton" data-category=""><img src="/landing-imgs/short-1.png" alt="" style="max-width: 100%;"></a>
          <a href="/user/category/#" class=" categoryButton" data-category=""><img src="/landing-imgs/short-3.JPG" alt="" style="max-width: 100%;"></a>
          <a href="/user/category/#" class=" categoryButton" data-category=""><img src="/landing-imgs/call.png" alt="" style="max-width: 100%;"></a>
          <a href="/user/category/#" class=" categoryButton" data-category=""><img src="/landing-imgs/short-4.GIF" alt="" style="max-width: 100%;"></a>
          <a href="/user/category/#" class=" categoryButton" data-category=""><img src="/landing-imgs/1.gif" alt="" style="max-width: 100%;"></a>
          <a href="/user/category/#" class=" categoryButton" data-category=""><img src="/landing-imgs/1.gif" alt="" style="max-width: 100%;"></a>

        </div>
</div>  -->

  <div class="container">

   <!-- Part 1: Products -->
   <% if (firstHalf && firstHalf.length > 0) { %>
    <% firstHalf.forEach(category => { %>
        <% if (category.products && category.products.length > 0) { %> <!-- Only render card if products are present -->
            <div class="category-card">
                <div class="top-cat">
                    <h3><%= category.categoryName %></h3>
                    <a href="/user/category/<%= category.categoryName %>#product-section" >See all</a>
                </div>

                <div class="products-grid">
                    <% category.products.forEach(product => { %>
                        <div class="product-item">
                            <a href="/user/product-details/<%= product.id %>" class="home-card">
                                    <img src="/uploads/<%= product.image %>" alt="<%= product.ProductName %>">

                                <p><%= product.ProductName %></p> 
                                <p>₦ <%= product.UnitPrice %></p>
                            </a>
                        </div>
                    <% }) %>
                </div>
            </div>
        <% } %> <!-- End of condition to render only if products exist -->
    <% }) %>
<% } %>

    <!-- Advertisement or other content -->
    <div class="advertisement">
        <p>Advertisement Area</p>
    </div>


      <!-- <div class="wide mt-5 mb-4">
        <a href="/user/category/#" class=" categoryButton" data-category=""><img src="/landing-imgs/wide.GIF" alt="" style="max-width: 100%;"></a>
      </div>
 -->



    <!-- Part 2: Products -->
    <% if (secondHalf && secondHalf.length > 0) { %>
      <% secondHalf.forEach(category => { %>
          <% if (category.products && category.products.length > 0) { %> <!-- Only render card if products are present -->
              <div class="category-card">
                  <div class="top-cat">
                      <h3><%= category.categoryName %></h3>
                      <a href="#">See all</a>
                  </div>

                  <div class="products-grid">
                      <% category.products.forEach(product => { %>
                          <div class="product-item">
                              <a href="/user/product-details/<%= product.id %>" class="home-card">
                                  <div class="image-wap">
                                      <img src="/uploads/<%= product.image %>" alt="<%= product.ProductName %>">
                                  </div>
                                  <p><%= product.ProductName %></p> 
                                  <p>₦ <%= product.UnitPrice %></p>
                              </a>
                          </div>
                      <% }) %>
                  </div>
              </div>
          <% } %> <!-- End of condition to render only if products exist -->
      <% }) %>
  <% } %>


      <%- include ('./partials/clients')%>


      <br>
      <br>
  
   
    </main>
  
  <%- include ('./partials/openFooter')%>
  
    <div id="preloader"></div>

  
  
  